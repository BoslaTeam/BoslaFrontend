import { mapAvailability } from '../mapper/map-availability';
import { mapLookup } from '../mapper/map-lookup';
import { mapReview } from '../mapper/map-review';
import { mapSpecialist } from '../mapper/map-specialist';
import { mapSpecialistDetails } from '../mapper/map-specialist-details';

export const SpecialistsMapper = {
    mapLookup,
    mapSpecialist,
    mapSpecialistDetails,
    mapReview,
    mapAvailability,
};